Logic for BugFile Mapper Java application:
1. get
    revs: a list of all the revisions and
    issues: a list of all issue ids
2. for every revision in revs{
      a. log: get commit log
      b. if log contains a number which matches with any ids in issues list then
          for every file f in this revision {
          add the matched issue id in the list of issues caused by the file f.
          }
   }

3. create a chart of the data collect